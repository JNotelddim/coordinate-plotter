const DEFAULT_SNAKES_STATE = ["Snake 1", "Snake 2"];
const snakesReducer = (state = DEFAULT_SNAKES_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default snakesReducer;
